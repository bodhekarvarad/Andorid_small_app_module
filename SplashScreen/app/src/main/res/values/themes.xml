<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SplashScreen" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SplashScreen" parent="Base.Theme.SplashScreen" />
    <style name="Theme.App.mySplashscreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorbg</item>//splashscreen background
        <item name="windowSplashScreenAnimatedIcon">@drawable/bjp_logo</item>//splashscreen logo
        <item name="postSplashScreenTheme">@style/Base.Theme.SplashScreen</item>
    </style>
</resources>